// remove
// const { nextI18NextRewrites } = require("next-i18next/rewrites");

const nextConfig = {
  i18n: {
    locales: ["en", "de"],
    defaultLocale: "de",
  },
};

module.exports = nextConfig;
